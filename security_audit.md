# セキュリティ監査レポート

**実施日**: 2026年3月1日  
**対象**: PDF→Excel 変換アプリ（FastAPI + React/Vite）

---

## 監査結果サマリ

| #   | セキュリティ項目          | ステータス | 対策概要                                      |
| --- | ------------------------- | ---------- | --------------------------------------------- |
| 1   | PDFアップロードサイズ制限 | ✅ 対応済  | フロント・バック両方で10MB制限                |
| 2   | タイムアウト設定          | ✅ 対応済  | フロント60秒 / バック keep-alive 120秒        |
| 3   | エラーハンドリング        | ✅ 対応済  | グローバル例外ハンドラ + ファイル名サニタイズ |
| 4   | HTTPS通信の確保           | ✅ 対応済  | 本番環境でHTTPSリダイレクト + 信頼ホスト制限  |
| 5   | CORS設定                  | ✅ 対応済  | メソッド・ヘッダー限定 + 環境変数対応         |
| 6   | セキュリティヘッダー      | ✅ 対応済  | 7種類のヘッダーをミドルウェアで付与           |

---

## 1. PDFアップロードサイズ制限

- **バックエンド**: `file.read()` 後に `len(contents) > 10MB` で検証、超過時は HTTP 413 返却
- **フロントエンド**: `file.size > 10MB` でサーバーへ送信前にブロック
- **リスク軽減**: 大容量ファイルによるDoS攻撃を防止

## 2. タイムアウト設定

- **フロントエンド**: `AbortController` + `setTimeout(60秒)` でリクエストタイムアウト
- **バックエンド**: uvicorn `timeout_keep_alive=120` で長いPDF処理に対応
- **リスク軽減**: 無限待機・接続滞留を防止

## 3. エラーハンドリング

- **グローバル例外ハンドラ**: 未処理例外を安全なJSONレスポンスに変換（スタックトレース非公開）
- **ファイル名サニタイズ**: `secure_filename()` でパストラバーサル攻撃を防止
- **ステータスコード別メッセージ**: フロントで 400/413/500 を区別して表示
- **エラーログ**: `logging` モジュールで詳細ログをサーバー側に記録

## 4. HTTPS通信の確保

- **HTTPSリダイレクト**: `ENVIRONMENT=production` 時に `HTTPSRedirectMiddleware` 有効化
- **信頼ホスト制限**: `TrustedHostMiddleware` でHostヘッダー攻撃を防止
- **HSTS**: `Strict-Transport-Security` ヘッダーで1年間HTTPS強制
- ⚠️ **注意**: SSL証明書の取得・設定はインフラ側で別途必要

## 5. CORS設定

- **オリジン**: 環境変数 `CORS_ORIGINS` で設定可能（デフォルト: `localhost:5173`）
- **メソッド制限**: `POST`, `GET` のみ許可（`DELETE`, `PUT` 等は拒否）
- **ヘッダー制限**: `Content-Type` のみ許可
- **プリフライトキャッシュ**: `max_age=600`（10分）

## 6. セキュリティヘッダー

全レスポンスに以下7ヘッダーを付与:

| ヘッダー                                      | 防御対象                     |
| --------------------------------------------- | ---------------------------- |
| `X-Content-Type-Options: nosniff`             | MIMEスニッフィング           |
| `X-Frame-Options: DENY`                       | クリックジャッキング         |
| `X-XSS-Protection: 1; mode=block`             | XSS（古いブラウザ）          |
| `Strict-Transport-Security`                   | HTTPダウングレード           |
| `Content-Security-Policy: default-src 'self'` | XSS / データインジェクション |
| `Referrer-Policy`                             | リファラー情報漏洩           |
| `Permissions-Policy`                          | カメラ・マイク等の不正利用   |

---

## 今後の推奨事項

| 優先度 | 推奨事項                                                      |
| ------ | ------------------------------------------------------------- |
| 🔴 高  | 本番環境でのSSL証明書設定（Let's Encrypt等）                  |
| 🟡 中  | レート制限の導入（短時間の大量リクエスト防止）                |
| 🟡 中  | アップロードファイルのウイルススキャン                        |
| 🟢 低  | アクセスログの外部監視サービス連携                            |
| 🟢 低  | CSPヘッダーの詳細化（フロントエンドのリソースに合わせて調整） |
